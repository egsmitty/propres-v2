// Global test setup.
//
// jest-dom matchers are additive and harmless in node-environment tests, so
// they are registered once here rather than imported in every DOM test file.
import '@testing-library/jest-dom/vitest';
